Based on the comparison, the only change is the version bump in package.json from 0.1.4-alpha to 0.1.5-alpha. Since there are no actual code changes or feature additions, here are the release notes:

---

## Patch Release

- Bump version to 0.1.5-alpha (no functional changes)